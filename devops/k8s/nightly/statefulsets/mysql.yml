apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: paylibero-db-mysql
spec:
  replicas: 1
  template:
    spec:
      containers:
        - name: mysql
          resources:
            requests:
              cpu: '1'
              memory: 2G
            limits:
              cpu: '1'
              memory: 2G
