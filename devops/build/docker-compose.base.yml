version: "2.0"

services:
  core-laravel:
    image: paylibero/build-core-laravel:${CI_COMMIT_SHORT_SHA}
    build:
      context: ../../lara-app
      dockerfile: Dockerfile
      args:
        - CI_COMMIT_SHORT_SHA
        - CI_COMMIT_MESSAGE
        - CI_PIPELINE_CREATED_AT
        - CI_PIPELINE_URL
